import assert from "node:assert/strict"; import { ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING, validateAccessControlPolicyAssessmentMapping } from "../lib/ai-documents/access-control-policy-assessment-mapping.ts";
assert.deepEqual(validateAccessControlPolicyAssessmentMapping(), []); assert.equal(ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING.filter(x=>x.theme==="organizational").length,34); assert.equal(ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING.filter(x=>x.theme==="people").length,17); assert.equal(ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING.filter(x=>x.theme==="technological").length,32); console.log("ACCESS CONTROL POLICY MAPPING QA: PASS");
