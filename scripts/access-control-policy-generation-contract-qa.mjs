import assert from "node:assert/strict";
import { ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING } from "../lib/ai-documents/access-control-policy-assessment-mapping.ts";
import { prepareAccessControlPolicyGenerationContext } from "../lib/ai-documents/access-control-policy-generation-contract.ts";
assert.equal(ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING.length,83); assert.equal(ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING.filter(x=>x.theme==='organizational').length,34); assert.equal(ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING.filter(x=>x.theme==='people').length,17); assert.equal(ACCESS_CONTROL_POLICY_ASSESSMENT_MAPPING.filter(x=>x.theme==='technological').length,32);
const input={workspace:{organizationName:'Org'},assessment:{context:{hasPrivilegedAccessRights:'no'},responses:[{theme:'organizational',controlId:'a5-18',questionId:'p5_18_004_privileged_rights',answer:'implemented'},{theme:'technological',controlId:'a8-5',questionId:'p8_5_001',answer:'implemented'},{theme:'physical',controlId:'a7-1',questionId:'p7_1_001',answer:'implemented'}]}};const c=prepareAccessControlPolicyGenerationContext(input);assert.equal(c.currentFacts.length,1);assert.equal(c.authentication.mfaRequirement,'unknown');assert.equal(c.accessRightsLifecycle.reviewFrequency,'unknown');assert.equal(Object.keys(c.sectionReadiness).length,16);assert.ok(!JSON.stringify({facts:c.currentFacts,trace:c.sourceTrace,authentication:c.authentication}).match(/VPN|PAM|JIT|RBAC|SIEM|password|token|secret/i));assert.deepEqual(c,prepareAccessControlPolicyGenerationContext(input));console.log('ACCESS CONTROL POLICY GENERATION CONTRACT QA: PASS');
